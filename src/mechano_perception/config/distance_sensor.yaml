# Distance sensor parameters

distance_sensor_node:
  ros__parameters:
    frame_id: "distance_sensor_link"
    device: "/dev/ttyUSB1"
    min_range: 0.02  # meters
    max_range: 2.0   # meters
    field_of_view: 0.1  # radians (~6 degrees)
    publish_rate: 10  # Hz
